# Lab 3.2: API Fetching with React

## 📝 Описание проекта

Приложение для загрузки и отображения данных пользователей из API. Проект демонстрирует работу с `useEffect` Hook, выполнение асинхронных запросов к внешнему API, обработку состояний загрузки и ошибок, а также правильную очистку побочных эффектов.

## 🎯 Функциональность

- **Загрузка данных пользователей** из JSONPlaceholder API
- **Выбор пользователя** - кнопки для быстрого переключения между пользователями 1, 2, 3
- **Случайный пользователь** - генерация случайного ID от 1 до 10
- **Отображение профиля** - имя, username, email, компания, город
- **Состояния загрузки** - индикатор процесса загрузки данных
- **Обработка ошибок** - корректное отображение сообщений об ошибках
- **Отмена запросов** - использование AbortController для отмены устаревших запросов

## 🏗️ Структура компонентов

```
App.jsx
└── UserApp.jsx (родительский компонент)
    └── UserProfile.jsx (дочерний компонент)
```

### Компоненты

#### `UserApp.jsx`
Родительский компонент, управляющий выбором пользователя:
- Управляет состоянием `userId`
- Предоставляет кнопки для выбора пользователя
- Кнопка "Случайный пользователь" генерирует случайный ID
- Передает `userId` в компонент `UserProfile`

#### `UserProfile.jsx`
Компонент для загрузки и отображения данных пользователя:

**Пропсы:**
- `userId` - ID пользователя для загрузки

**Состояния:**
- `user` - объект с данными пользователя
- `loading` - флаг процесса загрузки
- `error` - сообщение об ошибке (если есть)

**Особенности:**
- Использует `useEffect` для автоматической загрузки при изменении `userId`
- Реализует `AbortController` для отмены предыдущих запросов
- Правильно обрабатывает состояния loading, error и success

## 🛠️ Технологии

- **React 19.2.0** - библиотека для построения интерфейса
- **Vite 7.2.4** - инструмент для сборки и разработки
- **JSONPlaceholder API** - тестовый REST API для получения данных
- **Fetch API** - встроенный метод для выполнения HTTP-запросов
- **AbortController** - API для отмены асинхронных операций
- **ESLint** - линтер для проверки качества кода

## 🚀 Установка и запуск

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для production
```bash
npm run build
```

### Предварительный просмотр production-сборки
```bash
npm preview
```

### Проверка кода
```bash
npm run lint
```

## 📚 Изученные концепции

### React Hooks
- **useEffect** - выполнение побочных эффектов (API-запросы)
- **useState** - управление локальным состоянием компонента
- **Массив зависимостей** - контроль повторного выполнения эффекта
- **Cleanup функция** - очистка ресурсов при размонтировании компонента

### Работа с API
- **Асинхронные запросы** - использование async/await
- **Fetch API** - выполнение HTTP GET-запросов
- **Обработка ответов** - парсинг JSON
- **Обработка ошибок** - try/catch блоки
- **Состояния загрузки** - управление UX при асинхронных операциях

### Продвинутые техники
- **AbortController** - отмена устаревших запросов при быстром переключении
- **Условный рендеринг** - отображение разных состояний UI
- **Props** - передача данных между компонентами

## 🎓 Учебные задачи

1. **Task 1** - Базовая загрузка данных пользователя с помощью useEffect
2. **Task 2** - Реализация AbortController для отмены запросов и cleanup функции
3. **Task 3** - Добавление userId в массив зависимостей для автоматической перезагрузки данных
4. **Task 3.3** - Передача userId как пропса из родительского компонента

## 💡 Особенности реализации

### Отмена запросов
При быстром переключении между пользователями предыдущие запросы автоматически отменяются с помощью `AbortController`, что предотвращает:
- Race conditions (конкуренцию запросов)
- Установку устаревших данных в состояние
- Ненужную нагрузку на сеть

### Обработка ошибок
Различаются два типа ошибок:
- `AbortError` - запрос был отменен (не показывается пользователю)
- Другие ошибки - проблемы сети или сервера (отображаются пользователю)

### UX состояния
- **Loading** - индикатор загрузки для информирования пользователя
- **Error** - красное сообщение об ошибке с деталями
- **Success** - карточка профиля с данными пользователя

## 🌐 API Endpoint

Используется публичный API JSONPlaceholder:
```
https://jsonplaceholder.typicode.com/users/{userId}
```

Доступны ID пользователей от 1 до 10.

## 📄 Лицензия

Учебный проект для изучения React и работы с API.
