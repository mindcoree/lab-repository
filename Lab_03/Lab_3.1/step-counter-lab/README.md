# Lab 3.1: Step Counter

## 📝 Описание проекта

Приложение-счетчик с настраиваемым шагом изменения и историей операций. Проект демонстрирует работу с React Hooks (`useState`), передачу пропсов между компонентами и управление состоянием.

## 🎯 Функциональность

- **Настраиваемый шаг** - каждый счетчик может иметь свой шаг изменения значения
- **Инкремент/декремент** - увеличение и уменьшение значения на заданный шаг
- **История изменений** - отображение последних 5 операций
- **Счетчик операций** - подсчет общего количества выполненных операций
- **Сброс** - возврат к начальному значению с очисткой истории
- **Несколько экземпляров** - демонстрация работы независимых счетчиков с разными параметрами

## 🏗️ Структура компонентов

```
App.jsx
└── CounterApp.jsx (родительский компонент)
    ├── StepCounter (initialValue=0, step=1)
    └── StepCounter (initialValue=10, step=5)
```

### Компоненты

#### `CounterApp.jsx`
Родительский компонент, который создает два независимых экземпляра счетчика с разными начальными значениями и шагами.

#### `StepCounter.jsx`
Основной компонент счетчика с следующими пропсами:
- `initialValue` (по умолчанию: 0) - начальное значение счетчика
- `step` (по умолчанию: 1) - шаг изменения значения

**Состояния:**
- `count` - текущее значение счетчика
- `history` - массив с историей изменений
- `operationCount` - количество выполненных операций

## 🛠️ Технологии

- **React 19.2.0** - библиотека для построения интерфейса
- **Vite 7.2.4** - инструмент для сборки и разработки
- **ESLint** - линтер для проверки качества кода

## 🚀 Установка и запуск

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для production
```bash
npm run build
```

### Предварительный просмотр production-сборки
```bash
npm preview
```

### Проверка кода
```bash
npm run lint
```

## 📚 Изученные концепции

- **useState Hook** - управление локальным состоянием компонента
- **Props** - передача данных от родительского компонента к дочернему
- **Деструктуризация пропсов** - извлечение значений из объекта props
- **Значения по умолчанию** - задание дефолтных значений для пропсов
- **Массивы в состоянии** - работа с историей изменений
- **Обработчики событий** - реакция на действия пользователя
- **Иммутабельность состояния** - правильное обновление состояния через setState

## 🎓 Учебные задачи

1. **Task 1** - Создание базового счетчика с useState
2. **Task 2** - Добавление истории операций и счетчика изменений
3. **Task 3** - Использование пропсов для создания переиспользуемого компонента
4. **Task 3.1** - Демонстрация независимой работы нескольких экземпляров компонента

## 💡 Особенности реализации

- История хранит последние значения счетчика (не операции)
- Новые значения добавляются в начало массива для удобного отображения
- Кнопка сброса возвращает счетчик в исходное состояние
- Каждый экземпляр счетчика работает независимо с собственным состоянием
- Стилизация выполнена через inline-стили для простоты демонстрации

## 📄 Лицензия

Учебный проект для изучения React.
